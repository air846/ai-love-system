spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_love_system?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

# 开发环境应用配置
app:
  jwt:
    secret: ai-love-system-jwt-secret-key-2024
    expiration: 86400000 # 24小时

  ai:
    zhipu:
      api-key: f75d72ba78684960807a3655306b6dca.Hrv8UWXj1d75pKKH
      model: glm-4-flash
      max-tokens: 2048
      temperature: 0.7

# 开发环境日志级别
logging:
  level:
    root: INFO
    com.ai.love: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
